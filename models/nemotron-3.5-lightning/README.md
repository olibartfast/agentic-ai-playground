# Nemotron 3.5 Lightning profile

Status: discovery only. Do not select hardware from announcements or analogy
with earlier Nemotron models.

Before adding a launch command, verify from NVIDIA's model card:

- canonical checkpoint and license;
- total and active parameters;
- supported precision and minimum VRAM;
- context length and chat/tool-call template;
- supported vLLM, SGLang, or NIM versions;
- tensor-parallel and GPU-topology requirements.

