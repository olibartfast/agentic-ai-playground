# OpenCode

Copy `opencode.example.json` to the project being evaluated, replace the model
identifier with the name returned by `/v1/models`, and export `MODEL_API_KEY`.
Keep the endpoint on loopback when using the SSH tunnel from the cloud runbook.

