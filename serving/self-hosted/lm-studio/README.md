# LM Studio

LM Studio is a self-hosted model source with a desktop-oriented workflow. It
belongs under serving because coding agents consume its API endpoint.

The application usually runs on a personal workstation, but the taxonomy does
not assume a physical location: the important property is that you operate the
runtime and choose the loaded model. Record the model identifier, quantization,
context size, API compatibility, and LM Studio version in benchmark results.
