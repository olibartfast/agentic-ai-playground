# SGLang server

Use this backend when the official model recipe or a measured benchmark favors
SGLang. Keep model-specific flags in the experiment record and expose the same
OpenAI-compatible API contract used by the other backends.

